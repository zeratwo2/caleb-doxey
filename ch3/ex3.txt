1. Существуют два способа для создания новой переменной. Какие?
использовать  var и название переменной или  название переменной с ":=" которая сама определит ее тип 

2. Какое будет значение у x после выполнения x := 5; x += 1?
6

3. Что такое область видимости и как определяется область видимости переменной в Go?
Места, где может использоваться переменная , называются областью видимости переменной, определяется блоками ( существует только 
внутри текущих фигурных скобках)

4. В чем отличие var от const?
var -  переменная, а  const + это константа , которая не изменяет значений после иницилизации